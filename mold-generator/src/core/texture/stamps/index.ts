/**
 * Phase 2: Anatomical detail stamps
 *
 * Each stamp implements:
 *   apply(vertices, normals, position, rotation, scale, params) → Float32Array (displacement per vertex)
 *
 * Stamps: GillPlate, EyeSocket, MouthLine, Nostril, PectoralBase, Vent
 */
export {};
