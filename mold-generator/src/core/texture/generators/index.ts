/**
 * Phase 2: Procedural texture generators
 *
 * Each generator implements:
 *   generate(vertices, normals, uvs, params, flowDirection) → Float32Array (displacement per vertex)
 *
 * Generators: CycloidScale, CtenoidScale, GanoidScale, MicroScale,
 *             FinRay, LateralLine, SkinBump
 */
export {};
